{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma db push --skip-generate --accept-data-loss && npm run db:seed && next build",
  "regions": ["fra1"]
}
